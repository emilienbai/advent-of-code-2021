const input = 'target area: x=138..184, y=-125..-71';

function main() {
    /* highest where y speed is 0, so it is the sum from 1 to 124. we have unlimited x steps*/
    const highest = (124 * 125) / 2
    console.log(highest);
}
main();